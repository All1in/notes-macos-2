import React from 'react';

const SearchBox = () => {
    return (
        <div>
            <input
                type='text'
                placeholder='Search...'
                // onChange={(event) => }
            />
        </div>
    );
};

export default SearchBox;
